server {
    listen 80;

    location /api/static/ {
        alias /usr/share/nginx/html/static/;
        expires max;
        add_header Cache-Control "public";
    }

    location /api/img/ {
        alias /usr/share/nginx/html/img/;
        expires max;
        add_header Cache-Control "public";
    }
}
